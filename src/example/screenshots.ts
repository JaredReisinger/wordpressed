// dummy code used for screenshots
import { Client } from '../index.js';

export async function dummy() {
  const client = new Client('xxx');
  client;

  // ===== route completion =====
  // const response = await client.get('/wp/v2/pag

  // ===== args completion =====
  // const response = await client.get('/wp/v2/pages', {

  // ===== response completion =====
  // const response = await client.get('/wc/v3/orders');

  // response.body.forEach((order) => {
  //   order.
}
